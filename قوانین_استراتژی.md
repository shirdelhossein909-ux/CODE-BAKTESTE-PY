# 📋 قوانین کامل استراتژی — استخراج مو‌به‌مو از کد

> این سند دقیقاً از فایل `run_backtest.py` (مغز مشترک بک‌تست و ربات لایو) استخراج شده است.
> کنار هر قانون، **شماره خط** کد آمده تا برای تغییر، مستقیم سراغ همان‌جا بروی.
> هر عددی که اینجا نوشته شده، عیناً همان عددی است که در کد اجرا می‌شود.

---

## بخش ۰ — تنظیمات بالای فایل (خطوط ۹ تا ۹۷)

| ثابت | مقدار فعلی | خط | معنی |
|---|---|---|---|
| `USE_M15` | `False` | ۱۲ | راستی‌آزمایی داخل کندل با M15 — خاموش |
| `BACKTEST_START` | `2022-11-22` | ۱۵ | شروع بازه‌ی بک‌تست |
| `BACKTEST_END` | `None` | ۱۶ | پایان بازه (None = تا انتهای دیتا) |
| `COMMISSION_SPREAD_MULT` | `0.5` | ۱۹ | کمیسیون رفت‌وبرگشت = ۰.۵ × اسپرد |
| `SWAP_SPREAD_MULT_PER_NIGHT` | `0.2` | ۲۰ | سواپ = ۰.۲ × اسپرد به ازای هر شب |
| `DEFAULT_ENTRY_OFF` | `-0.50` | ۲۸ | ورود = ۵۰٪ داخل زون |
| `DEFAULT_RR` | `3.0` | ۳۵ | حد سود = ۳ برابر ریسک |
| `DEFAULT_DIST_CANCEL_R` | `0.0` | ۴۷ | قانون لغو با دور شدن قیمت — غیرفعال |
| `DEFAULT_MIN_RISK_ATR` | `0.0` | ۵۸ | فیلتر حداقل اندازه‌ی زون — غیرفعال |
| `PORTFOLIO_MAX_OPEN` | `5` | ۶۱ | سقف پوزیشن باز در شبیه‌سازی پرتفوی |
| `PORTFOLIO_RISK_PER_TRADE` | `0.005` | ۶۲ | ریسک هر معامله در پرتفوی = ۰.۵٪ |
| `WRITE_DETAILS` | `False` | ۶۶ | ساخت فایل جزئیات حرفه‌ای |
| `DEFAULT_MANAGE` | `"partial2"` | ۸۶ | مدیریت معامله = سیو سود در 2R |
| `MANAGE_TRIGGER_R` | `2.0` | ۸۷ | آستانه‌ی سیو سود = ۲ برابر ریسک |
| `PARTIAL_CLOSE_FRAC` | `0.5` | ۸۸ | سهم سیو سود = نصف حجم |

**پارامترهای پیش‌فرض تابع `backtest_one` (خطوط ۵۶۷–۵۷۱):**
`entry_off=0.10` ، `sl_off=0.25` ، `rr=3.0` ، `reserve=0.15` ، `risk_per_trade=0.01` ، `max_orders=3`

⚠️ نکته‌ی مهم: `entry_off` در امضای تابع `0.10` است ولی هنگام فراخوانی از `main` مقدار `DEFAULT_ENTRY_OFF = -0.50` پاس داده می‌شود. **مقدار مؤثر: ۰.۵۰-**

---

## بخش ۱ — اندیکاتورها (خطوط ۳۰۲ تا ۳۶۹)

### ۱.۱ ATR (خط ۳۰۲)
```
دوره = 14
TR = بیشینه‌ی سه مقدار: (های−لو) ، |های − کلوزِ قبلی| ، |لو − کلوزِ قبلی|
ATR = میانگین ساده‌ی ۱۴ کندل اخیر TR
```
تا وقتی ۱۴ کندل کامل نباشد، ATR مقدار ندارد (NaN).

### ۱.۲ فیلتر رنج (خط ۳۱۱)
```
lookback = 20 کندل   |   k = 3.0
رنج است اگر:  (بالاترین های ۲۰ کندل − پایین‌ترین لو ۲۰ کندل)  <  3 × ATR
```
یعنی وقتی کل دامنه‌ی ۲۰ کندل اخیر از ۳ برابر ATR کوچک‌تر باشد → بازار رنج → **معامله ممنوع**.

### ۱.۳ سوئینگ‌ها (خط ۳۲۷) — `n = 1`
- **سوئینگ‌های:** کندلی که های آن از های ۱ کندل قبل **و** ۱ کندل بعد بیشتر باشد (اکیداً بزرگ‌تر).
- **سوئینگ‌لو:** کندلی که لو آن از لو ۱ کندل قبل **و** ۱ کندل بعد کمتر باشد (اکیداً کوچک‌تر).

### ۱.۴ روند / چنج‌آف‌کاراکتر (خط ۳۳۹)
این همان بخش «تأییدیه‌ی تغییر ساختار» است:
```
سوئینگ‌ها با تأخیر ۱ کندل تأیید می‌شوند (بدون نگاه به آینده)
اگر close > آخرین سوئینگ‌های تأییدشده  →  روند = +1 (صعودی)
اگر close < آخرین سوئینگ‌لو تأییدشده   →  روند = -1 (نزولی)
در غیر این صورت روند قبلی حفظ می‌شود؛ مقدار اولیه = 0 (بدون روند)
```
یعنی تغییر روند فقط با **بسته شدن قیمت آن‌سوی آخرین سوئینگ** ثبت می‌شود — نه با شکست سایه.

---

## بخش ۲ — تعریف کندل‌ها (خطوط ۳۷۱ تا ۳۸۸)

| نوع | شرط دقیق | خط |
|---|---|---|
| **کندل بیس** | `بدنه ≤ 0.5 × (های−لو)` | ۳۷۱ |
| **دوجی کوچک** | `بدنه ÷ (های−لو) ≤ 0.20` **و** `(های−لو) ≤ 0.80 × ATR` | ۳۷۷ |
| **کندل تأیید (قوی)** | `بدنه > 0.5 × (های−لو)` | ۳۸۴ |

(در هر سه، اگر های−لو صفر یا منفی باشد → false)

---

## بخش ۳ — ساخت زون (خطوط ۴۱۲ تا ۴۶۵)

### ۳.۱ الگوریتم
از ابتدای دیتا، برای هر نقطه‌ی شروع `i`، طول بیس `L` را از **۱ تا سقف** امتحان می‌کند:
- **سقف طول بیس:** H4 → **۶ کندل** | W1 → **۱۲ کندل** (خطوط ۶۲۹–۶۳۰)
- همه‌ی کندل‌های بیس باید «کندل بیس» باشند؛ اولین کندلی که نبود، حلقه می‌شکند.
- کندل بعدی (`i+L`) باید **کندل تأیید (قوی)** باشد.
- **شرط شکست:**
  - صعودی (BUY): `close کندل تأیید > بالاترین های بیس`
  - نزولی (SELL): `close کندل تأیید < پایین‌ترین لو بیس`
- اولین `L` که همه‌ی شرط‌ها را داشته باشد، زون می‌سازد و `i` به `i + L + 1` می‌پرد؛ وگرنه `i` یکی جلو می‌رود.

### ۳.۲ خطوط پراکسیمال و دیستال (خطوط ۴۳۸–۴۵۳)
اگر **همه‌ی** کندل‌های بیس «دوجی کوچک» باشند (`doji_shadow = True`) از سایه‌ها، وگرنه از بدنه‌ها:

| حالت | زون خرید (BUY) | زون فروش (SELL) |
|---|---|---|
| دوجی‌شدو | پراکسیمال = بالاترین **های** بیس<br>دیستال = پایین‌ترین **لو** بیس | پراکسیمال = پایین‌ترین **لو** بیس<br>دیستال = بالاترین **های** بیس |
| عادی | پراکسیمال = بالاترین **بدنه** (max از open/close)<br>دیستال = پایین‌ترین **لو** بیس | پراکسیمال = پایین‌ترین **بدنه** (min از open/close)<br>دیستال = بالاترین **های** بیس |

- **زمان تولد زون** = زمان کندل تأیید (`i+L`)
- **ارتفاع زون** = `high − low` زون (خط ۷۳۶)

### ۳.۳ ادغام زون‌های هم‌پوشان (`dedup_zones_pit`، خط ۴۹۶)
- **آستانه‌ی هم‌پوشانی:** `thr = 0.55` (نسبت اشتراک به اجتماع دو زون)
- زون‌ها به ترتیب زمان بررسی می‌شوند؛ زون جدید که با زون قبلیِ فعال ≥۵۵٪ هم‌پوشانی داشته باشد:
  - زون قدیمی از **همان لحظه‌ی تولد زون جدید** باطل می‌شود (`superseded_time`)
  - محدوده‌ی زون جدید با زون قدیمی **تنگ‌تر** می‌شود: `low = max(low‌ها)` و `high = min(high‌ها)`

---

## بخش ۴ — حلقه‌ی اصلی: شرط‌های هر کندل H4 (خطوط ۸۶۵ تا ۸۸۹)

**ترتیب اجرا در هر کندل، دقیقاً به همین ترتیب است:**
۱) خروج پوزیشن‌های باز → ۲) لمس و انقضای زون‌ها → ۳) ثبت سفارش جدید → ۴) لغو هفتگی → ۵) لغو دور شدن → ۶) پر شدن سفارش‌ها

### شرط‌های ورود به حلقه
- `di < 1 or i < 1` → رد شدن از این کندل (خط ۸۷۲)
- اگر فیلتر رنج روزانه یا ۴ساعته هنوز آماده نیست (NaN) → **معامله ممنوع** (خط ۸۷۸)

### منابع داده‌ی تصمیم (بدون نگاه به آینده)
| متغیر | از کجا خوانده می‌شود | خط |
|---|---|---|
| روند روزانه `dtr` | کندل روزانه‌ی `di-1` (آخرین کندل کامل‌شده) | ۸۸۱ |
| روند ۴ساعته `htr` | کندل H4 شماره‌ی `i-1` (کندل قبلیِ بسته‌شده) | ۸۸۲ |
| رنج روزانه `drg` | کندل روزانه‌ی `di-1` | ۸۸۴ |
| رنج ۴ساعته `hrg` | کندل H4 شماره‌ی `i-1` | ۸۸۵ |
| بدنه‌ی کندل قبلی | `open/close` کندل `i-1` (برای چک هفتگی) | ۸۸۸ |
| های/لو کندل قبلی | برای چک «دور شدن قیمت» | ۸۸۹ |

---

## بخش ۵ — لمس زون و انقضا (خطوط ۹۰۱ تا ۹۳۷)

- زون فقط وقتی فعال است که `created_time < t` (کندل تأیید باید بسته شده باشد) — خط ۹۰۴
- زونی که `superseded_time` آن رسیده باشد → منقضی می‌شود — خط ۹۰۸
- **تعریف لمس:** `های کندل ≥ کف زون` **و** `لو کندل ≤ سقف زون` (خط ۹۱۵)

### شمارش تست‌ها
| رویداد | شرط | خط |
|---|---|---|
| **تست ۱** | اولین لمس زون | ۹۱۷ |
| **تست ۲ (و بعدی)** | باید حداقل **۳ کندل پاک** (بدون لمس) از تست قبل گذشته باشد **و** فاصله از آخرین لمس **≤ ۵۰ کندل** باشد | ۹۲۲ |
| شمارنده‌ی کندل پاک | هر کندلی که زون را لمس نکند، `clean_after_touch` یکی زیاد می‌شود | ۹۳۱ |
| **انقضای زون** | اگر بعد از تست ۱، بیش از **۵۰ کندل** بگذرد و تست بعدی نیاید → زون منقضی | ۹۳۳ |

---

## بخش ۶ — شرط‌های ثبت سفارش (خطوط ۹۳۹ تا ۹۸۵)

سفارش فقط وقتی ثبت می‌شود که **همه‌ی** این شرط‌ها برقرار باشند (به ترتیب بررسی):

| # | شرط | اگر نقض شود | خط |
|---|---|---|---|
| ۱ | زون قبلاً استفاده نشده باشد (`used`) و منقضی نباشد | رد | ۹۴۱ |
| ۲ | `touch_count < 3` | رد: **تست سوم ممنوع** | ۹۴۳ |
| ۳ | `touch_count ≥ 1` (زون حداقل یک بار لمس شده) | صبر (نه رد) | ۹۴۸ |
| ۴ | **نه** رنج روزانه **و نه** رنج ۴ساعته | رد: رنج | ۹۵۱ |
| ۵ | `dtr ≠ 0` **و** `htr ≠ 0` **و** `dtr == htr` (هم‌جهتی کامل روند) | رد: عدم هم‌جهتی | ۹۵۷ |
| ۶ | تعداد سفارش‌های فعالِ پرنشده `< max_orders (=3)` | رد: سقف سفارش | ۹۶۳ |
| ۷ | (اختیاری، فعلاً خاموش) اندازه‌ی زون ≥ `min_risk_atr × ATR` | رد: زون کوچک | ۹۷۱ |

**شماره‌ی تست سفارش:** اگر `touch_count == 1` → تست ۱، در غیر این صورت → تست ۲ (خط ۹۸۱)

---

## بخش ۷ — قیمت‌های سفارش (تابع `make_order`، خطوط ۷۳۵ تا ۷۵۹)

```
ارتفاع = سقف زون − کف زون          (اگر ≤ 0 → 1e-9)

زون خرید (BUY):
    ورود  = پراکسیمال + entry_off × ارتفاع      → با entry_off = -0.50 یعنی: پراکسیمال − 0.50×ارتفاع
    استاپ = دیستال   − sl_off × ارتفاع          → با sl_off = 0.25 یعنی: دیستال − 0.25×ارتفاع
    ریسک  = ورود − استاپ
    تارگت = ورود + rr × ریسک                    → با rr = 3

زون فروش (SELL):
    ورود  = پراکسیمال − entry_off × ارتفاع      → یعنی: پراکسیمال + 0.50×ارتفاع
    استاپ = دیستال   + sl_off × ارتفاع          → یعنی: دیستال + 0.25×ارتفاع
    ریسک  = استاپ − ورود
    تارگت = ورود − rr × ریسک
```
🔸 اسپرد در قیمت ورود لحاظ **نمی‌شود** (`eff_entry = entry`) — هزینه‌ها جداگانه از نتیجه کسر می‌شوند.

---

## بخش ۸ — لغو سفارش پیش از پر شدن

| قانون | شرط دقیق | وضعیت | خط |
|---|---|---|---|
| **زون مخالف هفتگی** | بدنه‌ی کندل H4 قبلی (`open`–`close`) با زون هفتگیِ **جهت مخالف** هم‌پوشانی داشته باشد | ✅ فعال | ۹۹۲–۱۰۰۲ |
| اعتبار زون هفتگی | فقط زون‌هایی که `created_time + ۷ روز ≤ t` و باطل نشده‌اند | — | ۹۸۹ |
| تعریف برخورد بدنه | `max(open,close) ≥ کف زون` و `min(open,close) ≤ سقف زون` | — | ۵۱۶–۵۱۸ |
| **دور شدن قیمت** | قیمت به اندازه‌ی `dist_cancel_r × ریسک` از ورود دور شود | ❌ خاموش (`0.0`) | ۱۰۰۴ |
| **رنج/عدم هم‌جهتی در لحظه‌ی پر شدن** | اگر همان لحظه رنج باشد یا روندها هم‌جهت نباشند | ✅ فعال | ۱۰۲۷ |

---

## بخش ۹ — پر شدن سفارش (خطوط ۱۰۳۵ تا ۱۰۴۷)

```
خرید:  اگر لو کندل ≤ قیمت ورود      →  پر می‌شود
فروش:  اگر های کندل ≥ قیمت ورود     →  پر می‌شود
```
(پر شدن **داخل** کندل مجاز است — منتظر کلوز نمی‌ماند)

**اندازه‌ی پوزیشن (خطوط ۱۰۵۱–۱۰۵۲):**
```
سرمایه‌ی قابل استفاده = اکویتی × (1 − reserve)      → reserve = 0.15
مبلغ ریسک = سرمایه‌ی قابل استفاده × risk_per_trade
```

---

## بخش ۱۰ — مدیریت و خروج معامله (تابع `process_pos_candle`، خطوط ۸۱۸ تا ۸۶۱)

### ۱۰.۱ سیو سود در 2R (حالت فعال: `partial2`)
```
آستانه (trigger):
    خرید:  ورود + 2.0 × ریسک
    فروش:  ورود − 2.0 × ریسک

وقتی های/لو کندل به آستانه برسد و در همان کندل استاپ لمس نشده باشد:
    banked = 2.0 × 0.5 = 1.0 R   ← نصف حجم نقد می‌شود
    frac   = 0.5                  ← نصف باقی‌مانده ادامه می‌دهد
    (فقط یک بار برای هر پوزیشن)
```

### ۱۰.۲ اولویت خروج (بدبینانه)
| حالت | نتیجه | خط |
|---|---|---|
| هم استاپ هم تارگت در یک کندل | **استاپ** (بدبینانه) | ۸۴۴–۸۵۴ |
| فقط استاپ | حد ضرر | ۸۵۵ |
| فقط تارگت | حد سود | ۸۵۹ |
| هیچ‌کدام | پوزیشن باز می‌ماند | ۸۶۱ |

⚠️ فعال شدن سیو سود فقط وقتی بررسی می‌شود که در همان کندل استاپ لمس **نشده** باشد (خط ۸۲۹).

### ۱۰.۳ محاسبه‌ی نتیجه (تابع `finalize_trade`، خطوط ۷۸۶ تا ۷۹۵)
```
raw_R    = (قیمت خروج − ورود) ÷ ریسک          [برای فروش برعکس]
نتیجه_R  = banked + frac × raw_R               [حالت عادی: banked=0 , frac=1]
هزینه    = (کمیسیون + سواپ × تعداد شب‌ها) ÷ ریسک
نتیجه‌ی نهایی = نتیجه_R − هزینه
اکویتی += مبلغ ریسک × نتیجه‌ی نهایی
```

### ۱۰.۴ پایان دیتا
پوزیشن‌های باز در انتهای دیتا با **قیمت close آخرین کندل** بسته می‌شوند («پایان دیتا»).

---

## بخش ۱۱ — نتایج ممکن هر معامله (با تنظیمات فعلی)

| سناریو | نتیجه R (قبل از هزینه) |
|---|---|
| استاپ بدون رسیدن به 2R | **−۱.۰** |
| رسیدن به 2R، بعد استاپ | ۱.۰ − ۰.۵ = **+۰.۵** |
| رسیدن به 2R، بعد تارگت ۳R | ۱.۰ + ۰.۵×۳ = **+۲.۵** |
| تارگت بدون ثبت 2R (نادر) | **+۳.۰** |

---

## بخش ۱۲ — سقف‌های اجرا (تابع `portfolio_replay`)

| قانون | مقدار در بک‌تست | مقدار در ربات لایو |
|---|---|---|
| سقف سفارش هم‌زمان هر نماد | ۳ (`max_orders`) | ۳ |
| سقف کل سفارش‌های در انتظار | — | ۸ (`MAX_PENDING_TOTAL`) |
| سقف کل پوزیشن‌های باز | `PORTFOLIO_MAX_OPEN` | ۸ (`MAX_OPEN_TOTAL`) |
| ریسک هر معامله | ۰.۵٪ | ۰.۵٪ |
| سرمایه‌ی رزرو | ۱۵٪ | ۱۵٪ |

---

## 🔧 راهنمای تغییر: هر قانون کجاست؟

| می‌خواهی این را عوض کنی | برو به |
|---|---|
| نقطه‌ی ورود (٪ عمق زون) | خط ۲۸ — `DEFAULT_ENTRY_OFF` |
| فاصله‌ی استاپ از دیستال | امضای تابع، خط ۵۶۸ — `sl_off=0.25` |
| نسبت حد سود | خط ۳۵ — `DEFAULT_RR` |
| آستانه و سهم سیو سود | خطوط ۸۷ و ۸۸ |
| دوره‌ی ATR | خط ۳۰۲ — `period=14` |
| فیلتر رنج (حساسیت) | خط ۳۱۱ — `lookback=20` و `k=3.0` |
| تعریف کندل بیس | خط ۳۷۵ — ضریب `0.5` |
| تعریف دوجی | خط ۳۷۷ — `body_ratio_max=0.20` و `range_atr_max=0.80` |
| تعریف کندل تأیید | خط ۳۸۸ — ضریب `0.5` |
| حداکثر طول بیس | خطوط ۶۲۹ (هفتگی=۱۲) و ۶۳۰ (چهارساعته=۶) |
| آستانه‌ی ادغام زون‌ها | خط ۴۹۶ — `thr=0.55` |
| فاصله‌ی لازم بین تست‌ها | خط ۹۲۲ — `clean_after_touch >= 3` |
| مهلت انقضای زون | خطوط ۹۲۲ و ۹۳۳ — عدد `50` کندل |
| ممنوعیت تست سوم | خط ۹۴۳ — `touch_count >= 3` |
| سقف سفارش هر نماد | امضای تابع، خط ۵۶۹ — `max_orders=3` |
| ریسک و رزرو | امضای تابع، خط ۵۶۹ — `reserve=0.15` ، `risk_per_trade=0.01` |
| اعتبار زون هفتگی | خط ۹۹۰ — `pd.Timedelta(days=7)` |

---

## ⚠️ نکات حیاتی هنگام تغییر

1. **ربات لایو از همین فایل قانون می‌گیرد** — هر تغییری اینجا، مستقیماً روی معاملات واقعی ربات اثر می‌گذارد.
2. بعد از هر تغییر: **اول بک‌تست بگیر، بعد ربات را ری‌استارت کن** — وگرنه رفتار لایو و بک‌تست از هم جدا می‌شود.
3. `entry_off` منفی = داخل زون. اگر مثبتش کنی، ورود می‌رود بیرون زون (که در بک‌تست ضررده بود).
4. سه قانون آماده ولی خاموش‌اند: فیلتر حداقل زون، لغو با دور شدن، محدودیت ضرر روز/هفته. هر سه در بک‌تست نتیجه را **بدتر** کردند.
5. `sl_off` و `entry_off` روی هم اثر ضربی دارند: ریسک واقعی = `ارتفاع × (1 + entry_off + sl_off)` — با مقادیر فعلی: `ارتفاع × 0.75`
